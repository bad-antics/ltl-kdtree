// ltl-kdtree/main.ltl
//
// K-d tree for k-NN queries in low-dimensional spaces.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-kdtree v0.1.0")
    io::println("K-d tree for k-NN queries in low-dimensional spaces.")
    0
}
