// examples/demo.ltl — runnable demo for ltl_kdtree

import std::io
use ltl_kdtree as lib

fn main() {
    io::println("ltl_kdtree demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}